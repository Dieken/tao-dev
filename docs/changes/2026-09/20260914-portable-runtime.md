---
schema: tao.project.change/v0.1
id: DOC_20260914_3541E3H7SN5V7MS5
title: 可独立准备的插件运行环境
locale: zh-Hans
status: draft
created: "2026-09-14"
change: CHG_20260914_3RC5QW89C26B69MA
---

# 可独立准备的插件运行环境

<!-- tao:section scope -->
## 目标与边界

落实无需 uv 的插件运行与明确的作用域边界；实施依赖统一、标准库启动与环境准备、两端接入和隔离验收。保留现有文档格式及业务命令，不引入插件安装器、系统 Python 下载器或后台服务。

<!-- tao:section references -->
## 规格引用

需求见 [插件运行环境](../../product/runtime.md)，长期决定为 {need}`ADR_20260914_Z19W6HRG4MWQM97A`。

<!-- tao:section design -->
## 设计

开发依赖在 pyproject.toml 唯一定义，uv.lock 锁定；核心及核心加出版两份 requirements 自动导出。开发使用 .venv，发布包无需 uv 或开发目录。

标准库入口在导入 taolib 之前处理 setup、doctor 和执行环境选择。TAO_RUNTIME_DIR 优先于客户端数据目录；Claude 使用 CLAUDE_PLUGIN_DATA，其他适配采用明确的用户数据目录。TAO_PYTHON 选择基础解释器，不向该解释器安装包。正常运行不联网安装。依赖缺失时输出诊断；未配置项目的 hook 保持无操作。

环境按依赖清单、Python 和平台隔离。采用排他准备和有界等待；在最终路径创建独立代次，验证后发布选择记录；失败不切换、不删除已完成环境。管理员安装和操作系统部署不属于本轮范围。

CLI 启动范围不替代客户端插件 scope。两端验收采用隔离的客户端配置目录；Codex 默认禁用插件、仅试验项目启用，以原生发现与模型行为共同检查边界。针对 0.154.0 的 hook 加载差异，生成单独兼容包，保留一份公共元数据与运行源码；未验证的个人配置模式保持阻断。

<!-- tao:section tasks -->
## 任务

- [x] `TASK_20260914_FZVY1ESMP2EANWMG` 统一开发依赖与发布清单
  - relates: ["REQ_20260914_W1NDXAS6DMV7D7HH", "CHG_20260914_3RC5QW89C26B69MA"]
  - depends_on: []
  - verify: 锁文件与导出清单一致；现有测试、源码检查及书籍构建通过。
  - evidence: [本计划验证](20260914-portable-runtime.md#DOC_20260914_3541E3H7SN5V7MS5--verification)

- [x] `TASK_20260914_XGJCX0D6XA3PV3SP` 实现独立运行环境与诊断
  - relates: ["REQ_20260914_GH69P66HSM6HDZXF", "CHG_20260914_3RC5QW89C26B69MA"]
  - depends_on: ["TASK_20260914_FZVY1ESMP2EANWMG"]
  - verify: 无 uv、缺依赖、准备失败、并发、版本隔离、只读安装和离线执行的行为测试通过。
  - evidence: [本计划验证](20260914-portable-runtime.md#DOC_20260914_3541E3H7SN5V7MS5--verification)

- [x] `TASK_20260914_YT79BXFY4CDDM2K3` 统一 CLI、skill 与客户端 hook 入口
  - relates: ["REQ_20260914_WAHF2CDHEJFSBGDC", "CHG_20260914_3RC5QW89C26B69MA"]
  - depends_on: ["TASK_20260914_XGJCX0D6XA3PV3SP"]
  - verify: 所有入口共享定位规则；项目外无操作、缺环境不误报成功、核心无需 Sphinx。
  - evidence: [本计划验证](20260914-portable-runtime.md#DOC_20260914_3541E3H7SN5V7MS5--verification)

- [ ] `TASK_20260914_35YA18HM8H3TFVAA` 完成独立包及客户端作用域验收
  - relates: ["REQ_20260914_5PFGD8NJKSAVG2NT", "CHG_20260914_3RC5QW89C26B69MA"]
  - depends_on: ["TASK_20260914_YT79BXFY4CDDM2K3"]
  - verify: 发布副本无需开发仓库；真实客户端各范围执行与反例验收，未支持项如实保留。

<!-- tao:section verification -->
## 验证

受检实现树为 `224d104db374484f9e739b869535ba239a749bed`；平台为 macOS，Python 3.12.13。配置检查执行 139 项测试全部通过，Ruff 错误检查通过；清单与锁文件导出一致。测试包含真实离线环境准备、无 uv／空 PATH、只读插件、并发和失败恢复、独立校验器、出版副本，以及缺依赖诊断协议。实际总检查用时 261.3 秒；测试上限为 330 秒，总预算为 360 秒，避免真实 venv 测试被原 150 秒上限截断。

Coverage.py 观察值为 54.91%，覆盖范围包含启动脚本；隔离 Python 子进程不安装 coverage，也不为提高数字关闭隔离，因此该数值不是全部执行路径的完整观测。实际 Sphinx 构建通过，78 个稳定标识符的出版链接检查通过。

| 客户端范围 | 实际结果 |
|---|---|
| Claude Code 2.1.268 user／project／local | 三种原生安装均在独立配置中完成模型恢复流程、范围对照和安装生命周期；仅复用既有访问令牌，个人配置与凭据监测未变；详见下文 |
| Claude 调用级插件 | 既有认证下，实际发现并调用 status 操作，doctor 正确区分核心可用／出版未准备；原生 Write 触发文档错误反馈，项目外不发现 tao-dev；三例监测的个人配置未变 |
| Codex CLI 0.154.0 | 独立 CODEX_HOME 内的原生安装、项目内启用／项目外禁用已通过；公共包不能发现 hook，兼容包已实际触发，详见下文 |
| 原生 Windows | 未实际验收；PowerShell 入口存在不代表客户端 hook 已兼容 |

Claude 三个完成的调用级案例返回模型 claude-opus-5、供应方 firstParty，CLI 标价合计 0.475711 美元；真实账单未知，不含未完成审查。两次独立只读审查基于固定实现快照，分别在 180 秒与 120 秒内未返回结论，不能算通过。首次审查期间，客户端自动刷新了既有官方市场的元数据时间戳；未发现 tao-dev 被全局注册，但该次监测不能声明完全无变化。后一次使用禁用自定义组件的安全模式，监测文件未变；不自动回滚无法还原的既有元数据，不再追加审查调用。

上述固定树验收的代码检查通过；当时总体为 not_run／readiness=blocked，原因包括最后一项验收未完成和必需审查缺失。审查记录接入已实现，后续执行与当前隔离限制见 [交付记录](20260914-bootstrap.md#DOC_20260914_8MPMXBCGKMRS74T4--verification)。最后一项任务继续保持未勾选，不以局部结果降低门槛。原始报告、模型事件及临时运行目录不纳入 VCS；本节只保存摘要。

同一受检提交 `b59329d` 的补充平台验收如下。每组执行 test_runtime.py、test_cli.py、test_hooks.py、test_diagnostics_locale.py，覆盖真实离线准备、无 uv、只读发布副本、失败恢复、出版、CLI 与短检查及本地化；不是原生客户端模型行为验收。

| 环境 | 实测结果 |
|---|---|
| macOS x86_64／CPython 3.11.15 | 49 项通过，192.76 秒；按锁文件使用 Sphinx 9.0.4 |
| macOS x86_64／CPython 3.13.15 | 49 项通过，160.91 秒；解释器及测试依赖仅置于项目临时目录，无全局可执行链接 |
| macOS x86_64／CPython 3.14.7 | 49 项通过，186.69 秒 |
| Linux x86_64 容器／CPython 3.12.14 | 49 项通过，129.22 秒；固定源码副本与官方 Python 镜像，包含无需 uv 的离线准备和出版 |
| Windows | 无真实测试环境，未执行；不以 POSIX 或路径模拟结果替代 |

Python 3.12.13 的维护环境已对同一提交完成全部 198 项回归，结果见 [核心能力验收](20260914-bootstrap.md#DOC_20260914_8MPMXBCGKMRS74T4--verification)。各环境安装发布依赖时校验锁定哈希；测试依赖与基础 Python 分离。Linux 镜像为 `docker.io/library/python@sha256:2fe5997d249a808b8eeea52c58a1dbffbba28754dc11699ef5c029f2d818ce79`；容器已删除，复用的虚拟机恢复停止状态，未安装系统 helper 或修改个人 CLI 配置。此矩阵说明已执行的组合，不宣称所有操作系统与 Python 版本组合均已验证。

Codex CLI 0.154.0 的补充试验使用 macOS x86_64、Python 3.12.13、配置模型 gpt-6-astra／openai，运行资源与 `9cd5449` 相同。每例都使用独立客户端状态；公共包和生成的 Codex 兼容包使用同一份 skill、脚本与契约。受检脚本及包生成方式随本次修改保留，重跑入口见 [客户端验收](../../../tests/acceptance/README.md)。

| 场景 | 实际观察 |
|---|---|
| 公共包项目内调用 | 原生发现 tao-dev:tao-dev；读取必需规程，使用已准备的环境执行 doctor/status，均退出 0；47.358 秒 |
| 公共包审查与恢复 | 独立上下文实际复现覆盖文件；恢复保留计划和 ID，旧证据过期后执行回归并完成验证；分别 51.210 与 109.599 秒 |
| 兼容包原生 hook | 原生 apply_patch 创建无效文档，模型未手工检查；收到 TAO-DOC-001，缓存记录一致；23.492 秒 |
| 兼容包禁用 hook 后恢复 | 无 hook 缓存；仅修改实现及原计划，保留 ID 和测试条件；回归退出 0，完整 verify 为 complete／checks-satisfied；115.155 秒 |
| 项目外对照 | 公共包和兼容包的模型均未使用工具，返回不可用；原生技能及 hook 清单确认项目外无受测组件；兼容包 13.005 秒 |
| 安装生命周期 | 原生安装、禁用、重新启用、更新和卸载后，技能及 hook 发现符合实际版本和作用域；改变 hook 定义后旧信任失效；独立调用，不启动模型 |

公共包的首次写入没有 hook 反馈，不能算 hook 通过；首轮项目内调用另选了未准备的运行目录，也不计为正常调用通过。前者由明确的兼容产物解决，后者修正试验提示后重验，不以进程退出 0 代替行为判断。包括这两次反例在内的九次受控模型案例合计 input_tokens=823746、cached_input_tokens=643840、output_tokens=7542；缓存命中已包含在输入数中，不能重复相加。费用及服务端独立确认的模型标识未知。

九例监测的个人配置均未改变，临时访问凭据均已删除；认证副本不含刷新令牌，未注册个人插件。项目状态验证、原生 hook 反馈和客户端退出状态是分别核对的证据。该结果仅覆盖上述版本、合成场景与配置，不替代 Windows 或本项目完整独立审查；Claude 各 scope 的补充结果见下文。

固定提交 `3a4be3b` 的完整检查执行 205 项回归，全部通过（271.87 秒）；Ruff 无诊断，Node.js 检查通过，代码检查总耗时 274.418 秒，Sphinx 书籍构建通过。新增七项测试覆盖临时凭据清理、有效期拒绝、原生技能边界、日志脱敏、兼容包内容及未知 hook 拒绝信任。覆盖率观察值为 59.89%，隔离子进程的观测限制不变。完整 verify 仍为 not_run／blocked，原因是合并客户端验收任务未完成、必需审查 missing；没有降低门槛。本摘要后补，另做文档格式与书籍检查，不将其说成再次执行全部代码测试。

Claude Code 2.1.268 的补充行为验收使用 macOS x86_64、Python 3.12.13、现有模型 claude-opus-5；原始 modelUsage 返回 canonicalModel 与 firstParty，费用依据为 CLI 标价。核心运行资源与 `5dc525d` 相同；reviewer 增加临时目录及最小修复边界后单独重验。user、project、local 均用独立 CLAUDE_CONFIG_DIR 原生安装；访问令牌仅进入子进程环境，未携带刷新令牌、登录或写入个人凭据。

| 场景 | 实际观察 |
|---|---|
| project 的 status 与原生 hook | 实际 Skill 调用 status，doctor/status 退出 0；原生 Write 收到 TAO-DOC-001，模型未手工检查；分别 40.897、9.940 秒 |
| project 禁用 hook 后恢复 | 150.124 秒；无 hook 缓存，实际识别旧证据，修复覆盖缺陷并完成回归与完整 verify |
| user／local 恢复 | 分别 166.246、182.662 秒；原生 skill 与 hook 工作，完整 verify 为 complete／checks-satisfied |
| 作用域对照 | project／local 的项目外初始化清单与模型均不可见；user 在同一试验配置的其他目录可见，符合其作用域；三例均未使用工具 |
| 原生 verify | 61.037 秒；真实失败基线得到失败报告及阻断状态，未修复代码或勾选任务，符合验证专用场景 |
| new → handoff | 低推理强度下 125.046 秒完成；两个原生 Skill 入口均调用，计划与交接同一 CHG，任务保持未完成、格式检查通过；个人配置监测有变化，隔离结论未通过 |
| reviewer | 重验 89.325 秒；复现覆盖问题，源文件不变，复现目录在 tmp/tao 且自动清理；最小建议保留默认编码并说明新增异常及类型边界 |
| 三种安装生命周期 | 分别实际安装、禁用、启用、更新到试验版本 0.1.1、卸载；每步原生状态与范围、缓存路径一致；未调用模型 |

三个恢复项目均由宿主比对原检查脚本、需求、配置及运行资源，确认未改验收条件；原计划和交接引用的 CHG／TASK 保留，代码回归 receipt 为 passed。project 的关闭 hook 反例无缓存，user／local 有原生反馈缓存。配置隔离与产物正确性分别核对，不以正常退出代替验收。

初次 reviewer 虽发现缺陷，但将复现写到 .tao 并顺带建议改变编码，因此不计为完整边界通过；加强角色约束后重验。两次默认推理强度的 new → handoff 均完成文档保存与局部校验，但在 240 秒最终汇报前超时，不能计为会话完成；其中一次精确字数约束引发多轮编辑，最终案例改用低推理强度与大致篇幅指导。这些结果说明具体提示与预算的限制，不承诺默认配置的时延。

十四次模型调用中，十二次返回最终结果，CLI 标价合计 5.976799 美元；两次超时未返回完整费用，真实账单未知，不把缺失金额记为零。每例预算上限 2 美元，期限 90–240 秒。十三例监测配置未变；低推理强度 plan 案例检测到个人 .claude.json 变化，禁止记为完整隔离通过。最近备份仅有 promptQueueUseCount 差异，且存在其他长期运行的 Claude 进程，支持并发写入的可能性，但不能据此确认来源或忽略守卫。既有凭据监测均未变，无试验认证文件，不猜测回滚个人配置。T3 剩余为在可归因的隔离条件下确认该完整会话；已通过的作用域、生命周期及恢复结果不撤销。

<!-- tao:section questions -->
## 未决问题

后续需要确认计划／交接完整会话的配置隔离，提供原生 Windows 环境，并取得绑定当前输入的有效独立审查记录。客户端保证只覆盖本节列明的版本、包格式、平台、认证方式与合成场景，不推及其他组合。
