---
schema: tao.project.evidence/v0.1
id: "DOC_20260919_4C9TQ4BP5ZPSXDAT"
title: "覆盖核对计划审查的主审裁决"
locale: "zh-Hans"
status: draft
created: "2026-09-19"
evidence: "EVD_20260919_6GV4E834DKRPWFFJ"
change: "CHG_20260919_CN6CCKSK3NPBCGMF"
recorded_at: "2026-09-19T10:05:55Z"
result: "failed"
coverage: "complete"
---

# 覆盖核对计划审查的主审裁决

<!-- tao:section scope -->
## 目标与边界

对本批第 1 轮独立文档审查提出的四项发现逐项裁决，并记录由裁决派生的计划修订。`result` 记为 failed，对应本轮审查结论 changes-requested；四项发现全部核实为真，无一项被驳回。

范围限于本批次第 1 轮的发现处置。不覆盖计划将要修改的参考页，不代表实现授权。

**审查执行时间与依据:** 第 1 轮于 2026-09-19T06:31:18Z 登记，报告同日返回，调度器记录约 10 分钟墙钟时间。裁决在报告返回后于同日完成。批次预算 7,200 秒，上限 2 轮，本裁决后剩余 1 轮。

<!-- tao:section inputs -->
## 输入与环境

<!-- tao:field fingerprint -->
**受检输入引用:** 与来源报告一致：基线提交 `3ea07df389c80b616a6eaaaa1735e48c93ec0dba` 加工作区未提交修改，受检文件为计划正文与月度导航。裁决派生的计划修订发生在本轮登记之后，属于第 2 轮定向复核的受检范围，不在本轮已绑定输入内。

<!-- tao:field environment -->
**环境:** 主审为 Claude Opus 5，供应商 Anthropic，即计划作者本人。裁决前对四项发现逐条独立复核，复核命令与结果记录在下方检查记录中，不以审查者结论直接采信。

| 报告 | 关注点 | 审查者、模型与供应商 | 调用方式与强度 | 独立性 | 轮次 |
|---|---|---|---|---|---|
| [独立文档审查](01-plan-claude.md) | 目标覆盖、隐藏决定、依赖与验证缺口、计划自身合规 | Claude Sonnet 5，Anthropic | 本会话子 agent，只读，普通强度，串行单审查者 | 独立上下文，跨模型，同供应商，非跨供应商 | 第 1 轮 |

<!-- tao:section checks -->
## 检查记录

<!-- tao:field command -->
**命令或观察步骤:** 对 D1 直接读取任务关系字段核对依赖图；对 D2 执行 `wc -c` 实测两份文件，以 `git diff --stat` 确认本工作树未改动它们，并以 `git show <修订>:<路径> | wc -c` 在产出原数值的提交上复测；对 R1 读取 `assets/document-profiles.json` 的 `entities` 与 `tasks` 节核对 `UC.required_options`、`ADR.required_options` 与 `tasks.relates_types`；对 S1 以 `grep -rn "TAO-LINK-002" docs/plans/2026-09/*.md` 核对历史批次记录原文。

<!-- tao:field expected -->
**预期:** 部分发现可能因契约误读或范围外而不成立。

<!-- tao:field observed -->
**实际观察:** 四项全部成立。D2 的实测值为 6,580 字节而非 6,537，且在原始提交上复测结果相同，确认为写下时即错误。R1 的注册表事实核对无误：`UC` 必须声明 `verifies` 且只能指向 `REQ`，`ADR` 无任何强制关系，`tasks.relates_types` 为 `["REQ", "CHG"]`。S1 的历史记录确认存在明确的保留判定。

<!-- tao:field reports -->
**报告:** 来源报告见 [独立文档审查](01-plan-claude.md)。

<!-- tao:section findings -->
## 发现与限制

<!-- tao:field limits -->
**限制:** 主审即计划作者，裁决不具备独立性；独立性由来源报告承担。四项发现的复核为确定性核对，但 R1 的后果判断仍是结构推断，未在真实事项上复现。本裁决不覆盖参考页内容，也不证明规则落地后有效。

| 来源发现 | 处置与范围 | 依据与理由 | 剩余工作或复查条件 |
|---|---|---|---|
| [D1](01-plan-claude.md) 收尾任务遗漏真实依赖 | 采用 | 复核确认图结构如报告所述。作者将「单独提交」与「无依赖」混为一谈：单独提交约束的是提交粒度，`depends_on` 约束的是执行顺序，收尾的一次性回归与 0 诊断断言确实依赖告警修正先完成 | 已把告警修正任务与载荷形状任务一并加入收尾任务的 `depends_on`；由第 2 轮复核 |
| [D2](01-plan-claude.md) 基线字节数与实测不符 | 采用 | 实测 6,580 字节，并在产出原数值的提交上复测得同样结果，确认该数字写下时即错误且被跨批次继承 | 计划两处已改为 6,580 并记录上一批有误及复核依据；上一批文档中的错误数值未回改，留待日后订正 |
| [R1](01-plan-claude.md) 覆盖对照看不见设计侧决定 | 部分采用 | 注册表事实与后果推断均成立，且正中用户原始观察里「设计侧」那一半，必须处理。但不采用报告所给的全量枚举适用设计文档 ADR 的做法：ADR 是长期决定，多数早已实现，全量枚举与 [文档规程](../../../../../../plugins/tao-dev/skills/tao-dev/references/documents.md) 既有的「覆盖本次范围」冲突，会把核对变成仪式 | 改为把覆盖对照左列定义为本次范围内的需求加本次新增或修改的设计决定，用例经 `verifies` 已归属需求不重复枚举。该方案经用户确认采纳；由第 2 轮复核 |
| [S1](01-plan-claude.md) 未回应此前的保留决定 | 采用 | 历史记录核对属实。用户明确要求构成正当重议依据，但计划未写明这是推翻既有判定，属可追溯性缺口 | 计划范围章节已补记该重议及其触发条件；由第 2 轮复核 |

### 未解决事项

上一批计划 [Skill 评审回归修正](../../../20260918-skill-review-followup.md) 中记录的 6,537 字节为错误数值，本次未回改该历史文档。历史执行结果不因此改写，但该数字不应被后续批次继续继承。

四项发现中的 D2 与 S1 同属一个模式：验证结论在批次之间被复制，而不是被重新测量或重新判断。已在计划验证章节加入防再犯规则，要求断言的具体数值来自本次实测或复核，引用历史批次结论时标明未复测。该规则本身的效果未度量。

**对下一步的影响:** 本轮结论为 changes-requested，处置已全部落入计划修订。计划修订发生在本轮输入绑定之后，因此需要第 2 轮定向复核覆盖这批改动。本裁决不构成实现授权，进入实现仍需用户明确批准。

<!-- tao:section retention -->
## 证据保存

<!-- tao:field retention -->
**保存位置与期限:** 本页与来源报告随计划长期保留。轮次登记与输入绑定保存在项目工作流状态中。复核命令的原始输出未长期保存，结论记录于本页检查记录。
